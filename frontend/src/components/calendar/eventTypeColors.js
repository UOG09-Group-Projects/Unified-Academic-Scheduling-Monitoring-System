// Shared event-type -> color mapping, used by every calendar view
// (mini dashboard calendar, month/week/day full calendar) so the same
// event type always reads as the same color everywhere.
export const TYPE_COLOR = {
  class:      '#3B82F6',
  assignment: '#F59E0B',
  exam:       '#EF4444',
  holiday:    '#22C55E',
  meeting:    '#A78BFA',
  personal:   '#64748B',
};
